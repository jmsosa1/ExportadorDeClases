<Window x:Class="ExportadorDeClases.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExportadorDeClases"
        mc:Ignorable="d"
        Title="Class Exporter to Json/CSV/Xml" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="LightGray" Padding="10" Grid.Row="0">
            <TextBlock Text="Exportador de Clases - Exporta clases de librerias a formatos JSON, CSV o XML" 
                       FontSize="18" FontWeight="Bold" Foreground="DarkBlue"/>
        </Border>
        <Border Grid.Row="1" Background="Gray" Margin="0,5,0,5">
            <TextBlock Text="Pasos" FontSize="16" FontWeight="Bold" Margin="10,0,0,10"/>
        </Border>
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="AliceBlue">
            <StackPanel Margin="10">
                <TextBlock Text="1 ) Indique el path del proyecto" Margin="10,5"></TextBlock>
                <StackPanel Orientation="Horizontal">

                    <TextBox Name="txtPathProyecto" Foreground="DarkBlue" IsReadOnly="True" Margin="10,0"
                             Width="400"></TextBox>
                    <Button Name="btnAbrirCarpeta" Click="BtnAbrirCarpeta_Click" TabIndex="1"  
                        Margin="10,0" Content="Buscar Proyecto" Width="150"></Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="3" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="AliceBlue">
            <StackPanel Margin="10">
                <TextBlock Text="2 )Indique el path del ensamblado compilado de la libreria de clase" 
                           Margin="10,5" FontSize="14"></TextBlock>
                <StackPanel Orientation="Horizontal">
                    <TextBox Name="txtPathAssambly" Foreground="DarkBlue" Margin="10,0" IsReadOnly="True" Width="400"></TextBox>
                    <Button Name="btnAbrirAssambly" Click="BtnAbrirAssambly_Click" TabIndex="1"  Width="150"
                             Margin="10,0"  Content="Buscar Ensamblado"></Button>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="4" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="AliceBlue">
            <StackPanel Margin="10" Orientation="Horizontal">
                <TextBlock Text="3) Cargar las clases del proyecto seleccionado" FontSize="14" Margin="10,5"/>
                <Button Content="Cargar clases" Click="OnLoadClassesClick" Width="150" Margin="10,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="5" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="AliceBlue">
            <StackPanel Margin="10" Orientation="Horizontal">
                <TextBlock Text="4) Seleccione la clase a exportar:" FontSize="14" Margin="10,5"/>
                <ComboBox x:Name="ClassSelector" Width="400" Margin="10,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="6" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Background="AliceBlue">
            <StackPanel Margin="10">
                <TextBlock Text="5) Seleccione el formato de exportación:" FontSize="14" Margin="10,5"/>
                <StackPanel  Orientation="Horizontal">


                    <ComboBox x:Name="FormatBox" Width="150" SelectedIndex="0" Margin="10,5">
                        <ComboBoxItem Content="JSON" />
                        <ComboBoxItem Content="CSV" />
                        <ComboBoxItem Content="XML" />
                    </ComboBox>
                    <Button Content="Exportar" Click="OnExportClick" Width="100" Margin="10,0"/>
                    <TextBlock x:Name="StatusBlock" Foreground="Green" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </Border>
      
           
            
            
           

           
          
           
        

    </Grid>
</Window>
